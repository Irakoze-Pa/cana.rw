import { useEffect, useState } from "react";
import { Users } from "lucide-react";
import api from "@/services/api";
type Customer = { _id: string; fullName: string; phone: string; email?: string };
export default function CustomersPage() { const [customers, setCustomers] = useState<Customer[]>([]); const [loading, setLoading] = useState(true); const [search, setSearch] = useState(""); useEffect(() => { void api.get<{ data: Customer[] }>("/users/customers").then((response) => setCustomers(response.data.data)).finally(() => setLoading(false)); }, []); const visible = customers.filter((customer) => `${customer.fullName} ${customer.phone} ${customer.email || ""}`.toLowerCase().includes(search.toLowerCase())); return <div className="space-y-6"><div className="rounded-3xl bg-slate-950 p-6 text-white sm:p-8"><div className="flex items-center gap-3"><span className="rounded-2xl bg-white/10 p-3 text-red-300"><Users size={24} /></span><div><p className="text-xs font-bold uppercase tracking-widest text-red-300">Sales workspace</p><h1 className="mt-1 text-2xl font-extrabold">Customer directory</h1><p className="mt-1 text-sm text-slate-300">Active customer accounts available for quotations, orders, and invoices.</p></div></div></div><input value={search} onChange={(event) => setSearch(event.target.value)} placeholder="Search customer name, phone or email" className="h-11 w-full max-w-md rounded-xl border border-gray-200 bg-white px-3 text-sm" /><div className="overflow-x-auto rounded-2xl border border-gray-200 bg-white"><table className="min-w-full text-left text-sm"><thead className="bg-gray-50 text-xs uppercase text-gray-500"><tr><th className="px-5 py-3">Customer</th><th className="px-5 py-3">Phone</th><th className="px-5 py-3">Email</th></tr></thead><tbody className="divide-y divide-gray-100">{loading ? <tr><td colSpan={3} className="p-8 text-center text-gray-500">Loading customers…</td></tr> : visible.map((customer) => <tr key={customer._id}><td className="px-5 py-4 font-semibold text-gray-900">{customer.fullName}</td><td className="px-5 py-4">{customer.phone}</td><td className="px-5 py-4 text-gray-500">{customer.email || "—"}</td></tr>)}{!loading && !visible.length && <tr><td colSpan={3} className="p-8 text-center text-gray-500">No customers found.</td></tr>}</tbody></table></div></div>; }
